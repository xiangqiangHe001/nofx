现在请分析并输出决策（先给出简短的思维链，然后给出严格的 JSON）。

排序与组合规则（强约束）：
- 同一轮决策必须“先平后开”：若需换仓，先输出 `close_*`，再输出 `open_*`
- 已有持仓的币种只能输出 `hold` 或 `close_*`；严禁再次 `open_*`
- 新开仓前自查：总保证金使用率 ≤ 60%，当前持仓数 ≤ 3；若不满足，输出 `wait` 而非 `open_*`
- 单仓位保证金默认 ≤ 60% 账户净值（`position_size_usd / leverage`）；除非“强信心”且给出充分理由，否则不要超过

新增周期规则（必须遵守）：
- 强制开仓逻辑（第 4 周期）仅在以下全部成立时触发：
- 1) 连续 3 个周期未开仓；2) 当前无任何持仓；3) 未触发“连续亏损保护（硬性）”；
- 若触发强制开仓 → 当轮仅输出一个 `open_*`，选择评分最高的币种与方向，并提供完整风控字段与理由  
- 若任一条件未满足 → 输出 `wait`，不得绕过风控
- 新开仓后需持有满 3 个周期：前 3 周期只允许 `hold` 或 `wait`；第 4 周期起才评估 `close_*`（除非触发强制止损或极端风控）

连续亏损保护（硬性）：
- 连续亏损 3 笔 → 暂停 2 小时
- 单笔亏损 ≥ 10%（保证金口径）→ 暂停 1 小时
- 当日总亏损 ≥ 20% → 暂停 2 小时
- 连续亏损 3 笔 且 当日亏损 ≥ 20% → 暂停 6 小时
- 暂停期间直接输出 `wait`，不进行任何分析或API请求
- 夏普比率 < -0.5 → 至少观望 3 个周期，仅允许 `hold/wait`

输出要求：
- 允许动作：`open_long`, `open_short`, `close_long`, `close_short`, `hold`, `wait`
- 对所有动作，必须给出：
- - `symbol`：交易币种，格式为 `"BTCUSDT"`（若 `close_*` 且仅有唯一对应持仓，允许留空）
- - `confidence`：0–1 置信度
- - `risk_usd`：本次决策风险敞口估算（美元）
- 当动作为 `open_long` 或 `open_short`，此外必须给出：
- - `position_size_usd`：开仓美元规模，正数
- - `leverage`：杠杆倍数，正数
- - `stop_loss`：止损价位（与该品种价格同一计量单位）
- - `take_profit`：一次性止盈价位（与该品种价格同一计量单位）

额外校验（请在思维链中说明结果）：
- 当前账户总保证金使用率（`MarginUsedPct`）≤ 60%，否则不要开新仓；若因“先平后开”释放保证金，请明确说明，并在 JSON 中体现先 `close_*` 再 `open_*` 的顺序
- 当前持仓数量 ≤ 3；若达到上限，输出 `wait`
- 单仓位保证金（`position_size_usd / leverage`）默认 ≤ 60% 账户净值；超过时需“强信心”且给出充分理由
- 若为第 4 周期强制开仓场景：必须在思维链中给出评分依据（多周期共振、MACD柱状图强度、价量结构、OTE、RSI、资金费率与OI、流动性），并明确说明满足触发条件

请严格输出如下 JSON 数组格式：
[
  {
    "action": "open_short",
    "symbol": "ETHUSDT",
    "position_size_usd": 1500,
    "leverage": 5,
    "stop_loss": 3620.5,
    "take_profit": 3350.0,
    "confidence": 0.82,
    "risk_usd": 180
  }
]

注意：
- 仅输出 JSON；分析与理由请放在前面的思维链部分
- 字段名与大小写必须与示例完全一致
- 若决定 `close_long` 或 `close_short`，仅需给出通用字段（可省略价位字段）
